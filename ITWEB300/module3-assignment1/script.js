// Assuming the login form has id attributes for username and password fields
let usernameInput = document.getElementById("username");
let passwordInput = document.getElementById("password");
let errorElement = document.getElementById("error-message");

// Check login credentials and display error if incorrect
if (/* Check login conditions */) {
    errorElement.innerText = "Incorrect username or password";
    usernameInput.value = ""; // Clear username field
    passwordInput.value = ""; // Clear password field
}





  
  